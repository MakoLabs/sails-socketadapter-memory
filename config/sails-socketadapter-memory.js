/**
 * Default configuration file for the Sails memory socket.io adapter. If using the memory adapter this file should be copied into the Sails project's config directory.
 * The memory adapter has no required configuration.
 */
module.exports = {
};